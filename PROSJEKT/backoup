/* Main */
main{
    border-radius: 12px;

    display: flex;
    flex-direction: column;
    align-items: center;

    margin: 12px;
}
/* Main */
/* Overskrift */
#overskrift{
    width: 80%;
    display:flex;
    justify-content: center;
}

/* Main */
/* Spill-container */
#spillContainer{
    display: flex;
    justify-content: space-around;
    flex-wrap: wrap;

    height: 80%;
    width: 80%;
}


/* Spill-container */
/* spillEl */
.spillDiv{
    background-color: #24252a;

    width: 300px;
    height: 200px;

    margin: 30px;

    border: solid 3px black;
    border-radius: 12%;

    overflow: hidden;
}
.spillTittel{
    background-color: rgb(67, 211, 240);

    font-family: "rowdies", sans-serif;
    color:white;
    font-weight: 300;
    font-size: larger;

    width:110%;
    height: 12%;
    padding-left: 12%;
}
.spillLink{
    display:flex;
    flex-direction:column;
    justify-content: flex-end;

    text-decoration: none;

    width:100%;
    height:100%;
}



/* // Henter elementer fra DOM
const spillContainer = document.querySelector('#spillContainer')
let imgContainer = document.querySelector('')

// Spillene; objekt
let dinoEl = {
    navn: "Dino",
    link: "DinoGame/dino.html",
    bgBilde: "bilder/Dino.png" 
}
let triviaEl = {
    navn: "Trivia",
    info: "Test your knowledge and challenge your friends in our thrilling trivia game! Dive into a world of fun facts and see if you can claim the title of trivia master! NB: be sure to check out the about-page for information regarding how the game should be played!", 
    link: "TriviaGame/trivia.html",
    bgBilde: "bilder/TriviaBg.png",
}


// Spillene; array
let spill = [dinoEl, triviaEl]


// Fyll spillcontainer
function LeggTilSpill(){
    for(let i = 0; i<spill.length; i++){
        // Lager html-elementene
        let divEl = document.createElement('div')
        let pEl = document.createElement('p')
        let linkEl = document.createElement('a') 

        // Setter riktig link, bilde og navn til spillene
        divEl.style.backgroundImage = `url(${spill[i].bgBilde})`
        divEl.style.backgroundSize = 'cover'
        
        linkEl.setAttribute('href', spill[i].link)
        pEl.innerHTML = spill[i].navn

        //Gir klasse til styling i css
        divEl.classList.add('spillDiv')
        linkEl.classList.add('spillLink')
        pEl.classList.add('spillTittel')

        //Legger de inn i hverandre
        divEl.appendChild(linkEl)
        linkEl.appendChild(pEl)

        //Legger til i spill container
        spillContainer.appendChild(divEl)
    }
}

LeggTilSpill() */